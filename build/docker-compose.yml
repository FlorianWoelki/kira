version: '3'
services:
  kira:
    image: florianwoelki/kira:latest
    ports:
      - '8080:8080'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ../.:/root/kira
    environment:
      APP_CONTAINER_PATH: /root/kira
